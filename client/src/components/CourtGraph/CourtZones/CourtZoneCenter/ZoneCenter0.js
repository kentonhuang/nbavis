import React from 'react';

const ZoneCenter0 = (props) => {
  return (
    <circle 
        cx={props.cx}
        cy={props.cy}
        r={props.r}
        style={{
        strokeWidth: '3px',
        stroke: '#ccc',
        fill: 'none'
        }}
    />
  );
};

export default ZoneCenter0;